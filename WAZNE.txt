todo
TASK [worker : Check if /etc/kubernetes/pki is empty before join] *************************************************************
[WARNING]: Skipped '/etc/kubernetes/pki' path due to this access issue: '/etc/kubernetes/pki' is not a directory



install ufw
add roule to ssh and 6443 port
sudo ufw allow 6443/tcp ...
enable ufw
suf reload jak potrzeba

tcp:
6443
2379
2380
10250
10259
10257
179

https://kubernetes.io/docs/tasks/administer-cluster/certificates/
https://kubernetes.io/docs/tasks/tls/managing-tls-in-a-cluster/
https://www.youtube.com/watch?v=gXz4cq3PKdg&ab_channel=CNCF%5BCloudNativeComputingFoundation%5D


on connect node:
openssl genrsa -out ca.key 2048
openssl req -x509 -new -nodes -key ca.key -subj "/CN=${MASTER_IP}" -days 10000 -out ca.crt
openssl genrsa -out server.key 2048

make csr.conf with content:
```
[ req ]
default_bits = 2048
prompt = no
default_md = sha256
req_extensions = req_ext
distinguished_name = dn

[ dn ]
C = <country>
ST = <state>
L = <city>
O = <organization>
OU = <organization unit>
CN = <MASTER_IP>

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = kubernetes
DNS.2 = kubernetes.default
DNS.3 = kubernetes.default.svc
DNS.4 = kubernetes.default.svc.cluster
DNS.5 = kubernetes.default.svc.cluster.local
IP.1 = <MASTER_IP>
IP.2 = <MASTER_CLUSTER_IP>

[ v3_ext ]
authorityKeyIdentifier=keyid,issuer:always
basicConstraints=CA:FALSE
keyUsage=keyEncipherment,dataEncipherment
extendedKeyUsage=serverAuth,clientAuth
subjectAltName=@alt_names
```

openssl req -new -key server.key -out server.csr -config csr.conf

Move csr firl to master node:

openssl x509 -req -in server.csr -CA ca.crt -CAkey ca.key \
    -CAcreateserial -out server.crt -days 10000 \
    -extensions v3_ext -extfile csr.conf -sha256

openssl req  -noout -text -in ./server.csr
openssl x509  -noout -text -in ./server.crt



On master node do:

cat <<EOF | kubectl apply -f -
> apiVersion: certificates.k8s.io/v1
kind: CertificateSigningRequest
metadata:
  name: fancyname
spec:
  request: $(cat server.csr | base64 | tr -d '\n')
  signerName: master1.my/serving
  usages:
  - digital signature
  - key encipherment
  - server auth
> EOF


k get csr
k certificate approve fancyname
k get csr -o yaml

    
CREATE KUBECONFIG
